<!DOCTYPE html>
<html lang="sw">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>PANDAS - Request Product</title>
    <style>
        body { font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; background-color: #f4f7f6; margin: 0; padding: 20px; display: flex; justify-content: center; }
        .container { background: white; padding: 30px; border-radius: 12px; box-shadow: 0 4px 15px rgba(0,0,0,0.1); width: 100%; max-width: 400px; }
        h2 { color: #2c3e50; text-align: center; margin-bottom: 10px; }
        p { text-align: center; color: #7f8c8d; font-size: 14px; margin-bottom: 25px; }
        label { display: block; margin-bottom: 5px; font-weight: bold; color: #34495e; }
        input, select, textarea { width: 100%; padding: 12px; margin-bottom: 15px; border: 1px solid #dcdde1; border-radius: 8px; box-sizing: border-box; }
        button { width: 100%; padding: 15px; background-color: #27ae60; color: white; border: none; border-radius: 8px; font-size: 16px; cursor: pointer; transition: 0.3s; }
        button:hover { background-color: #219150; }
        .success-msg { color: #27ae60; text-align: center; display: none; margin-top: 15px; font-weight: bold; }
    </style>
</head>
<body>

<div class="container">
    <h2>🐼 PANDAS Sourcing</h2>
    <p>Unatafuta Spare au Nguo na huipati? Tuandikie hapa tuitafute kwa ajili yako!</p>
    
    <form id="requestForm">
        <label>Jina Lako</label>
        <input type="text" id="name" placeholder="Ingiza jina lako" required>

        <label>Namba ya WhatsApp</label>
        <input type="tel" id="phone" placeholder="Mfano: 0712XXXXXX" required>

        <label>Unatafuta nini?</label>
        <select id="category">
            <option value="Car Spares">Car Spares (Vifaa vya Magari)</option>
            <option value="Fashion">Fashion (Nguo na Viatu)</option>
            <option value="Electronics">Electronics</option>
            <option value="Other">Vinginevyo</option>
        </select>

        <label>Maelezo ya Bidhaa</label>
        <textarea id="details" rows="4" placeholder="Elezea kwa kina bidhaa unayotafuta..." required></textarea>

        <button type="submit" id="submitBtn">Tuma Uhitaji Wangu</button>
    </form>
    <div id="successMsg" class="success-msg">✅ Ombi lako limepokelewa! Tunaitafuta sasa hivi.</div>
</div>


<script>
    document.getElementById('requestForm').addEventListener('submit', async (e) => {
        e.preventDefault();
        const btn = document.getElementById('submitBtn');
        btn.innerText = "Inatuma...";
        btn.disabled = true;

        const data = {
            name: document.getElementById('name').value,
            phone: document.getElementById('phone').value,
            category: document.getElementById('category').value,
            details: document.getElementById('details').value
        };

        try {
            // 1. Inahifadhi kwenye Database (PostgreSQL)
            const response = await fetch('https://stics.onrender.com/api/request-product', {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify(data)
            });

            if (response.ok) {
                // 2. Inatengeneza Ujumbe wa WhatsApp
                const nambaYako = "255693546400";
                const ujumbe = `*PANDAS SOURCING REQUEST*\n\n` +
                               `*Jina:* ${data.name}\n` +
                               `*WhatsApp:* ${data.phone}\n` +
                               `*Kundi:* ${data.category}\n` +
                               `*Maelezo:* ${data.details}`;
                
                const whatsappUrl = `https://wa.me/${nambaYako}?text=${encodeURIComponent(ujumbe)}`;

                // 3. Inafungua WhatsApp na Reset fomu
                window.open(whatsappUrl, '_blank');

                document.getElementById('requestForm').reset();
                document.getElementById('successMsg').style.display = 'block';
                btn.innerText = "Tuma Uhitaji Wangu";
                btn.disabled = false;
            } else {
                alert("Kuna tatizo kwenye server. Jaribu tena.");
                btn.disabled = false;
            }
        } catch (error) {
            console.error(error);
            alert("Imeshindwa kuunganisha. Hakikisha umei-push kodi ya server kwanza.");
            btn.disabled = false;
        }
    });
</script>

git add .
git commit -m "Added WhatsApp notification for Sado"
git push origin main
git add .
​git commit -m "Added product request feature"
​git push origin main

